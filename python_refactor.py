import re

with open(r'f:\webdev\รวมโปรเจคของโรงเรียน\school-finance-dashboard\components\CashBookReport.tsx', 'r', encoding='utf-8') as f:
    content = f.read()

# I will just write a simpler script to extract CashBookAddModal completely
# But given the complexity of the previous attempt, I will just do it by hand via view_file
